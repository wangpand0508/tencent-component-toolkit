export const WebServerImageDefaultPort = 9000;

export enum VersionOperation {
  Publish = 'publish',
  Delete = 'delete',
}

export enum AliasOperation {
  Create = 'create',
  Update = 'update',
  Delete = 'delete',
}
